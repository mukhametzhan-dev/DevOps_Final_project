# --- Изменено для тестовой среды ---
ENVIRONMENT=local
PROJECT_NAME="DevOps Final Project Test"
FIRST_SUPERUSER=admin@test.com
FIRST_SUPERUSER_PASSWORD=testpassword

# --- Добавлено, если отсутствует в Settings ---
ACCESS_TOKEN_EXPIRE_MINUTES=60

# --- Ключевое изменение: Использование SQLite для тестов ---
SQLALCHEMY_DATABASE_URI="sqlite:///./test.db"

# --- Скопировано из вашего .env ---
DOMAIN=localhost
FRONTEND_HOST=http://localhost:5173
STACK_NAME=full-stack-fastapi-project
BACKEND_CORS_ORIGINS="http://localhost,http://localhost:5173,http://127.0.0.1,http://127.0.0.1:5173,https://localhost,https://localhost:5173,https://127.0.0.1,https://127.0.0.1:5173,http://localhost.tiangolo.com"
SECRET_KEY=VhHalfB1oKoJYjHAIN1qoNQeFjSbOK00ExTZok6Nuxc
# Emails
SMTP_HOST=
SMTP_USER=
SMTP_PASSWORD=
EMAILS_FROM_EMAIL=info@example.com
SMTP_TLS=True
SMTP_SSL=False
SMTP_PORT=587
# Postgres (оставляем, т.к. они обязательны в Settings, но переопределены выше)
POSTGRES_SERVER=localhost
POSTGRES_PORT=5432
POSTGRES_DB=app
POSTGRES_USER=postgres
POSTGRES_PASSWORD=adminpassword
SENTRY_DSN=
DOCKER_IMAGE_BACKEND=backend
DOCKER_IMAGE_FRONTEND=frontend